============
Installation
============

At the command line::

    $ easy_install django-mptt-comments

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mptt-comments
    $ pip install django-mptt-comments
